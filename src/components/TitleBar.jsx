import './TitleBar.css'

function TitleBar() {
  return (
    <div class="title-bar">
      <div class="title-bar-content">
        <h1 class="app-title">Solid.js PWA</h1>
        <div class="title-bar-actions">
          {/* Add action buttons here if needed */}
        </div>
      </div>
    </div>
  )
}

export default TitleBar
